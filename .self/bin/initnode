#!/bin/sh

npm --registry=https://registry.npm.taobao.org install -g babel-cli stylelint uglify eslint browser-sync webpack webpack-dev-server mocha electron gulp browserify nodemon yo generator-generator generator-node
